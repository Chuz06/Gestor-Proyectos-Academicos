@model IEnumerable<Gestor_Proyectos_Academicos.Models.ReporteEstudianteView>

@{
    ViewData["Title"] = "Reporte por Estudiantes";
}

<link rel="stylesheet" href="~/css/Reportes.css" />

<div class="section-header">
    <h2 class="section-titulo">Reporte por Estudiantes</h2>
</div>

<div class="card-table-container">
    <table class="table">
        <thead>
            <tr>
                <th>Estudiante</th>
                <th>Proyecto</th>
                <th>Total Tareas</th>
                <th>Tareas Completadas</th>
                <th>Avance</th>
            </tr>
        </thead>

        <tbody>
            @foreach (var e in Model)
            {
                <tr>
                    <td>@e.Estudiante</td>
                    <td>@e.Proyecto</td>
                    <td>@e.TotalTareas</td>
                    <td>@e.TareasCompletadas</td>
                    <td>@e.PorcentajeAvance.ToString("0.##") %</td>
                </tr>
            }
        </tbody>
    </table>
</div>
